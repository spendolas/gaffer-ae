{
  "name": "gaffer-daemon",
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "//test": "Auto-discovery (no path arg). Do NOT pass 'test/': Node 22+ treats a bare directory arg to --test as a module entry and fails with MODULE_NOT_FOUND on Windows; a glob would break Node 20. No path works on both.",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "acorn": "^8.18.0",
    "ws": "^8.17.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.18.1",
    "esbuild": "^0.28.0",
    "pkg": "^5.8.1"
  }
}
